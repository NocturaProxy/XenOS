// determine favorite apps for taskmenu

export default window.xen.favorites = {
  async init() {
    // TODO: init favorite app stuff
  },

  opened(id) {
    return id;
  },

  closed(id) {
    return id;
  },
};
